# For real use, create this with `kubectl create secret generic ...` or a
# sealed-secrets/External Secrets pipeline instead of committing plaintext.
# Values below are base64 of: root=rootpass123, todo=todopass
apiVersion: v1
kind: Secret
metadata:
  name: mysql-credentials
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: cm9vdHBhc3MxMjM=
  MYSQL_PASSWORD: dG9kb3Bhc3M=
stringData:
  MYSQL_DATABASE: tododb
  MYSQL_USER: todo
